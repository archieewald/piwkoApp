export const getBeers = state => state.beers.beers;
export const checkIfMoreBeers = state => state.beers.isThereMoreBeers;
export const hasBeersFetchFailed = state => state.beers.hasFetchFailed;
export const areBeersLoading = state => state.beers.isLoading;
